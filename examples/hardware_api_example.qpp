#include <iostream>
#include "qpp/hardware_stub.hpp"

int main() {
  qpp::HardwareStub hw;

  // Emit a few QIR instructions
  hw.emit("H 0");
  hw.emit("CX 0,1");
  hw.emit("MEASURE 0,1");

  // Display the collected program
  std::cout << hw.result();

  return 0;
}
